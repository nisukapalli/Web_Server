# Web_Server
A web server implemented with BSD sockets. Dumps HTTP request messages to console. File names handed in case insensitive fashion. Supports htm/html, txt, jpg/jpeg, and png file types. If file exists and can be opened successfully, server reads data and sends contents to client in HTTP response. Else, throws 404 not found error.
